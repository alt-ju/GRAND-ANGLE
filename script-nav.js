/* const btn = document.getElementById('brg-btn')
btn.addEventListener('click', openNav())

    function openNav() {
        let items = document.querySelectorAll('.hidden-items')
        if (items.style.display === 'none') {
            items.classList.add('.active')
        } else {
            items.classList.remove('.active')

        }
    } */

/* const btn = document.getElementById('burger').addEventListener('click', function () {
    let items = document.querySelectorAll('.hidden-items span')
    items.forEach(item => {
        if (item.style.display === 'none') {
            item.classList.add('.open')
        } else {
            item.classList.remove('.open')
        }
    }) 
})

console.log(btn) */